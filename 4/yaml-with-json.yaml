$schema: http://json-schema.org/draft-07/schema#
type: object
properties:
  name:
    type: string
  version:
    type: string
    pattern: "^\\d+\\.\\d+\\.\\d+$"
  replicas:
    type: integer
    minimum: 1
required:
  - name
  - version
